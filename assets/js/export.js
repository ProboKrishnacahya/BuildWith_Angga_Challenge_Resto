// * Start of Export Animate On Scroll Library's initialization.
export let initAOS =
    AOS.init({
        duration: 750 // Set the duration of animation to 750ms
    });

export let refreshHardAOS = AOS.refreshHard(); // Reinit `array` with AOS elements and trigger refresh to called on `Document Object Model` changes that are related to aos elements
// * End of Export Animate On Scroll Library's initialization.